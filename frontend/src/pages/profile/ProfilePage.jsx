import { Envelope, CalendarBlank } from '@phosphor-icons/react'; import { Card } from '../../components/ui/Card.jsx'; import { useAuth } from '../../hooks/useAuth.js';
function formatDate(value){if(!value)return '—';const date=new Date(value);return Number.isNaN(date.getTime())?'—':new Intl.DateTimeFormat('pt-BR',{dateStyle:'long'}).format(date);}
export function ProfilePage(){const {user}=useAuth();return <div className="page"><header className="page-heading"><p className="eyebrow">Sua conta</p><h1>Perfil</h1><p>Confira os dados vinculados à sua jornada no Habitus.</p></header><Card className="profile-card"><div className="profile-avatar">{user?.name?.charAt(0)?.toUpperCase()}</div><div><h2>{user?.name}</h2><p>Conta Habitus</p></div><dl><div><dt><Envelope/> E-mail</dt><dd>{user?.email}</dd></div><div><dt><CalendarBlank/> Data de cadastro</dt><dd>{formatDate(user?.createdAt)}</dd></div></dl></Card></div>}
