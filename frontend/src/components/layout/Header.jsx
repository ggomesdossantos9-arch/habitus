import { List, SidebarSimple } from '@phosphor-icons/react'; import { ThemeToggle } from '../ui/ThemeToggle.jsx'; import { useAuth } from '../../hooks/useAuth.js';
export function Header({ onMenu, onCollapse, collapsed, menuOpen }) { const { user } = useAuth(); return <header className="app-header"><button className="icon-button menu-button" onClick={onMenu} aria-label="Abrir menu" aria-controls="app-sidebar" aria-expanded={menuOpen}><List size={24}/></button><button className="icon-button collapse-button" onClick={onCollapse} aria-label={collapsed ? 'Expandir menu' : 'Recolher menu'} aria-controls="app-sidebar"><SidebarSimple size={22}/></button><div className="header-spacer"/><ThemeToggle/><div className="user-chip"><span className="avatar" aria-hidden="true">{user?.name?.charAt(0)?.toUpperCase()}</span><span>{user?.name}</span></div></header>; }
